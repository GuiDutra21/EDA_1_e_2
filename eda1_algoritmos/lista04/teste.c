# include <stdio.h>
# include <stdlib.h>
# define swap(a,b) {int t; t = a; a = b; b = t;}

int main()
{   
    int numeroCandidato;
    while((scanf("%d",&numeroCandidato)) != EOF);
    while(1);
    return 0;
}